
var playerturn = true;

function placeGameState(state) {
    var gameState = document.getElementById('gameState');
    gameState.innerHTML = state;
}

while (false) {
    placeGameState("There is no game state");
    if (playerturn) {
        placeGameState("Please Select a Pawn");
        if (pawnSelected = true) {
            placeGameState("Please roll the dice.");
        }

    } else {


    }
}


